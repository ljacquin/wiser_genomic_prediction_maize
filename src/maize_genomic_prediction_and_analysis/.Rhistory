styler:::style_active_file()
print('Not enough memory to use optimize_whitening_and_regularization()
defaulting to wiser default values whitening_method = "ZCA-cor"
and alpha_ = 0.01')
styler:::style_active_file()
message("Insufficient memory to run optimize_whitening_and_regularization().
Defaulting to wiser default values:
whitening_method = 'ZCA-cor' and alpha_ = 0.01.")
styler:::style_active_file()
median(c(1,2,3))
IQR
